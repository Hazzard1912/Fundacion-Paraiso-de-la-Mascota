---
import Layout from "../layouts/Layout.astro";
import { actions } from "astro:actions";

const { data, error } = await Astro.callAction(actions.getNoticiasData, {});
const noticias = Array.isArray(data) ? data : Array.isArray(data?.data) ? data.data : [];

function formatDate(dateString: string) {
  const date = new Date(dateString);
  return date.toLocaleDateString("es-CO", {
    year: "numeric",
    month: "long",
    day: "numeric",
  });
}
---

<Layout title="Noticias - Paraíso de la mascota Cali">
  <section class="bg-amber-50 py-20">
    <div class="max-w-6xl mx-auto px-4">
      <div class="text-center mb-16">
        <h1 class="text-4xl md:text-5xl font-bold text-amber-900 mb-4">Noticias</h1>
        <p class="text-lg text-gray-700 max-w-2xl mx-auto">
          Entérate de las últimas novedades, historias y actividades de la fundación.
        </p>
      </div>
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-10">
        {noticias.length === 0 && (
          <div class="col-span-full text-center text-gray-500">No hay noticias disponibles.</div>
        )}
        {noticias.map((noticia: any) => (
          <a href={`/noticias/${noticia.slug}`} class="group bg-white rounded-2xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 border border-amber-100 flex flex-col">
            <div class="aspect-[16/9] overflow-hidden bg-amber-100 flex items-center justify-center">
              {noticia.imagen?.url ? (
                <img src={noticia.imagen.url} alt={noticia.imagen.alternativeText || noticia.titulo} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" />
              ) : (
                <div class="w-full h-full flex items-center justify-center text-amber-400 text-4xl">📰</div>
              )}
            </div>
            <div class="p-6 flex flex-col flex-grow">
              <h2 class="font-bold text-2xl mb-2 text-amber-800 group-hover:text-amber-600 transition-colors line-clamp-2">{noticia.titulo}</h2>
              <p class="text-gray-600 text-sm mb-4 line-clamp-3 flex-grow">{noticia.resumen}</p>
              <div class="flex items-center justify-between mt-auto pt-2">
                <span class="text-amber-700 font-medium text-xs">{formatDate(noticia.fecha)}</span>
                <span class="text-amber-600 font-medium text-xs group-hover:text-amber-900 transition-colors">Leer más →</span>
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>
</Layout> 