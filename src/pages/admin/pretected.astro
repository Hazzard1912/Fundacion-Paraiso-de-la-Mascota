---
// Se pueden hacer validaciones para proteger paginas especificas desde esta seccion.
// Para eso, y como la pagina se debe mostrar bajo demanda, se debe tener o bien el proyecto en modo SSR o bien en modo SSG.
// En este caso, como estamos en hybrid, necesitamos que la pagina sea renderizada en el servidor.

export const prerender = false;

// Si la pagina no es prerenderizada, se puede hacer una redireccion a otra pagina.
const Authenticated = false;

if (!Authenticated) {
    return Astro.redirect('/admin/login');
}

// Aqui continuaria el contenido de la pagina.
const title = 'Protected Page';
const username = 'admin';

// Si lo hacemos desde el middleware(que es como voy a trabajar en este proyecto)
// En el middleware podemos hacer la validacion y construir un objeto que luego obtendremos en algo llamado "locals"

---

